<UserControl x:Class="ChatBot.Components.NewConversationDetails.ConversationDetails"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ChatBot.Components.NewConversationDetails"
              xmlns:viewModel="clr-namespace:ChatBot.MVVM.ViewModel"        
             mc:Ignorable="d" 
             Height="60" Width="500">

    <Border CornerRadius="10" Background="White">
        <Grid Margin="8">
            <TextBox     
                x:Name="ConversationNameTextBox"
                    Width="{Binding Width,RelativeSource={RelativeSource AncestorType=UserControl}}"
                    Height="{Binding Height,RelativeSource={RelativeSource AncestorType=UserControl}}"
                    FontSize="28"
                     Background="Transparent"
                     BorderThickness="0"
                     TextChanged="TextBox_TextChanged"/>

        </Grid>

    </Border>

</UserControl>
